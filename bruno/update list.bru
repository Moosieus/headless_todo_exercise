meta {
  name: update list
  type: http
  seq: 6
}

put {
  url: {{host}}/api/lists/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "id": 14,
    "title": "An updated title!"
  }
}

vars:pre-request {
  id: 14
}
